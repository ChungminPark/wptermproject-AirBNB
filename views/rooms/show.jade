extends ../layout

block content
  .jumbotron-reservation
  
  .container
    .col-md-10 
      br
      h1 #{room.title}
      p #{room.city} 
      .float-right
       p #{room.email}
    br
    .row
      .col-md-10 
        hr
        h3 숙소
        p #{room.capacity} 명
        hr
        h3 시설
        p #{room.content}
        hr
        h3 가격
        p #{room.fee} 원
        hr
        h3 설명
        p #{room.content1}
        hr
        h3 숙소 이용규칙
        p #{room.content2}
        
        hr
        h2 호스트: #{room.email}님
        h3.description #{room.city}
      .col-md-12  
        hr
      .col-md-12
        #map(style='width:100%; height:50em; margin-top:10%')
        script(type='text/javascript', src='//apis.daum.net/maps/maps3.js?apikey=66b9028c8fdb984ec5bddac45fa84c5e')
        script.
          var container = document.getElementById('map');
          var options = {
            center: new daum.maps.LatLng(33.450701, 126.570667),
            level: 3
          };
          var map = new daum.maps.Map(container, options);
        br
    a.btn.btn-default(href='/rooms/lists') 목록
    a.btn.btn-default(href='/books/#{room._id}/') 예약하기


    script(type='text/javascript').
      var container = document.getElementById('map'), //지도를 담을 영역의 DOM 레퍼런스
      options = { //지도를 생성할 때 필요한 기본 옵션
        center: new daum.maps.LatLng(33.450701, 126.570667), // 지도의 중심좌표
        level: 3 // 지도 확대, 축소 레벨
      };
      //수정필요 
      var map = new daum.maps.Map(container, options); //지도 생성 및 객체 리턴
      // 주소-좌표 변환 객체를 생성합니다
      var geocoder = new daum.maps.services.Geocoder();

      
      script(type='text/javascript', src='//apis.daum.net/maps/maps3.js?apikey=66b9028c8fdb984ec5bddac45fa84c5e')
      
      script(type='text/javascript', src='//apis.daum.net/maps/maps3.js?apikey=APIKEY&libraries=LIBRARY')
      // services 라이브러리 불러오기
      script(type='text/javascript', src='//apis.daum.net/maps/maps3.js?apikey=APIKEY&libraries=services')
      // services와 clusterer, drawing 라이브러리 불러오기
      script(type='text/javascript', src='//apis.daum.net/maps/maps3.js?apikey=APIKEY&libraries=services,clusterer,drawing')
